{
  "name": "smartnovap-varmeflyt-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static interaction prototype for the Varmeflyt field-service platform.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs",
    "check": "node scripts/build.mjs && node --check dist/app.js"
  },
  "engines": {
    "node": ">=22"
  }
}
